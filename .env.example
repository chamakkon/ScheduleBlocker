# Daily Visual Planner — optional environment variables
#
# Google OAuth / Slack tokens are stored via the in-app Settings UI
# (and in userData in production). You usually do not need a .env file.
#
# If Electron’s download cache causes permission issues, you can use a
# project-local cache (example):
# ELECTRON_CACHE=.cache/electron
