# Contributing

## 開発の流れ

1. Node.js 20 以上を用意する
2. `npm ci` で依存関係をインストール
3. `npm run dev` で開発モード起動
4. 変更後は `npm run build` が通ることを確認

## プルリクエスト

- 可能なら issue で相談してから大きな変更をしてください
- `data/` や認証情報を含むファイルは **コミットしない** でください（`.gitignore` 済み）

## コミットメッセージ

- 日本語・英語どちらでも構いませんが、変更内容が分かるようにしてください
